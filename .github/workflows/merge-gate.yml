# PUBLIC — every PR triggers this workflow and the gate/merge check must be green to merge.
name: Merge Gate

on:
  pull_request:
    branches: [ main, develop ]

permissions:
  contents: read
  security-events: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  # None of the three reusable calls below take `secrets: inherit`: none of them
  # declares a `secrets:` block or reads `secrets.*`, and `inherit` would hand them
  # every secret this repo has, bypassing declaration. GITHUB_TOKEN reaches them
  # automatically. release.yaml is the only caller that genuinely needs inherit.
  build:
    uses: octopusden/octopus-base/.github/workflows/common-java-gradle-build.yml@v2.5.1
    with:
      java-version: '25'
      flow-type: 'hybrid'
      docker-image: components-management-portal

  quality:
    uses: octopusden/octopus-base/.github/workflows/common-java-gradle-quality-gates.yml@v2.5.1
    with:
      java-version: '25'

  security:
    uses: octopusden/octopus-base/.github/workflows/common-java-gradle-security-reports.yml@v2.5.1
    with:
      java-version: '25'
      # CodeQL runs on PRs. It used to be off here because the repo-level *default
      # setup* covered PRs, but that setup is disabled (it blocks advanced SARIF
      # uploads), so without this PRs would get no code scanning at all.
      enable-codeql: true
      # Both inputs below are mandatory, not cosmetic. The reusable workflow
      # defaults to `java-kotlin` alone, so omitting the language list would
      # silently drop javascript-typescript and actions coverage; and the category
      # must match security.yml exactly, or GitHub treats PR and main analyses as
      # different configurations and stops correlating alerts.
      codeql-languages: java-kotlin,javascript-typescript,actions
      codeql-category: /portal-codeql
      trivy-continue-on-error: true

  frontend:
    # Contract-drift gates only. Deliberately does NOT run lint / typecheck /
    # test:coverage: build.gradle.kts wires npmLint, npmTypecheck and
    # npmTestCoverage into the `check` task, so the `build` job above already
    # executes all three. Running them here as well made every PR do the frontend
    # checks twice over.
    name: frontend / contract-drift-gates
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          # Keep in exact parity with the gradle-node pin in build.gradle.kts.
          node-version: '24.16.0'
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      # Wave 0 drift gate. Regenerates schema.d.ts from the vendored
      # v4.json and `diff`s against the committed copy — any uncommitted
      # delta between the spec and the generated types fails the PR.
      # Without this the script existed only as a local command and
      # contributors could re-vendor v4.json without regenerating.
      - run: npm run generate-types:check
      # Cross-repo staleness gate. `generate-types:check` only proves
      # schema.d.ts matches the *vendored* v4.json — not that the vendored
      # copy still matches CRS's published spec. This fetches CRS's spec at
      # the pinned ref (see scripts/vendor-spec.sh) and fails if Portal's
      # copy has fallen behind, so the contract can't silently drift between
      # the two repos. `gh` is preinstalled on the runner; GH_TOKEN lets it
      # read the public CRS repo without hitting the anonymous rate limit.
      - run: npm run vendor-spec:check
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  gate-merge:
    name: gate/merge
    needs: [ build, quality, security, frontend ]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: octopusden/octopus-base/.github/actions/merge-gate@v2.5.1
        with:
          needs: ${{ toJson(needs) }}
