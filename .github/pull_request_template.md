## What

<!-- One paragraph: what changes for a user of the Portal, and why. -->

## Spec

<!--
This repo is spec-first: a behaviour change must update a specification, and
that spec must be committed *before* the implementation. The `spec / gate`
check enforces both. See AGENTS.md § Spec-first for the paths it watches.

Link the spec document (or the openspec/ change) this PR implements.
-->

Spec:

<!--
No spec needed? Apply the `no-spec-impact` label — that, and only that, skips
the gate. Use it for changes with no observable behaviour: refactors, tests,
build/CI, dependency bumps, docs. A checkbox here does nothing; the opt-out is
a label so that it is visible on the PR, recorded in the timeline, and can be
applied by a reviewer rather than only self-declared.
-->


## Verification

<!-- Commands run, and what you checked by hand. -->
